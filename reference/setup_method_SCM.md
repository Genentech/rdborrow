# Setup method SCM (Deprecated)

\`r lifecycle::badge("deprecated")\`

This function has been removed. Use \[scm()\] instead.

## Usage

``` r
setup_method_SCM(...)
```

## Arguments

- ...:

  Ignored.

## Value

This function is defunct and always signals an error; it does not return
a value.

## Examples

``` r
try(setup_method_SCM())
#> Warning: 'setup_method_SCM' has been removed. Use scm() instead.
#> Error : setup_method_SCM() is no longer functional. Use scm().
```
